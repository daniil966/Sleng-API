My Slang Dictionary — REST API — это простое Go-приложение для сбора и управления сленговыми словами. Оно позволяет добавлять, просматривать и удалять сленговые выражения через консольный интерфейс или REST API. Все данные сохраняются в локальном файле slang.json. Приложение поддерживает базовую систему пользователей (регистрация и вход), но API пока не защищён авторизацией — доступен всем. Требуется Go 1.21+. <br><br>

API работает на порту 8080. Все эндпоинты возвращают JSON. Слово считается уникальным без учёта регистра: например, «Краш» и «краш» — одно и то же. Удаление происходит не по названию, а по числовому индексу (начиная с 1): /api/entries/1 удаляет первое слово в списке.<br><br>

Для регистрации нового пользователя отправьте POST-запрос на /api/register с JSON-телом, содержащим поля username и password (пароль должен быть не короче 4 символов). После регистрации можно войти через /api/login, указав те же логин и пароль. Информацию о текущем пользователе (без пароля) можно получить по /api/user.<br><br>

Работа со словарём: список всех слов доступен по GET /api/entries. Чтобы добавить новое слово, отправьте POST /api/entries с JSON, содержащим как минимум word и meaning; опционально можно указать example, origin и synonyms (массив строк). Пример тела: {"word": "зашквар", "meaning": "Что-то постыдное", "example": "Этот поступок — полный зашквар", "origin": "из тюремного жаргона", "synonyms": ["позор", "срам"]}. Удалить слово можно через DELETE /api/entries/{index}, где {index} — номер слова в списке (начиная с 1).<br><br>

Данные хранятся в slang.json в формате: объект с полями user (логин и пароль в открытом виде!), version (всегда "1.0") и entries — массивом записей. Каждая запись содержит word, meaning, example, а также необязательные origin и synonyms.<br><br>

Важные особенности: пароли хранятся в открытом виде (не для продакшена!), API не проверяет, авторизован ли пользователь (в отличие от консольного режима), уникальность слова проверяется через strings.EqualFold, а удаление — только по индексу, не по названию. Для потокобезопасной работы с файлом используется sync.RWMutex.<br><br>

В будущем можно улучшить приложение: добавить JWT-авторизацию, поиск по слову, редактирование записи, категории, пагинацию и валидацию входных данных. Пока это минималистичный, но полностью рабочий словарь современного сленга — идеальный учебный проект или основа для личного использования. Запускайте, добавляйте слова и изучайте язык улиц! 💬✨

