🗣️ My Slang Dictionary — REST API
Простое приложение для сбора и управления сленговыми словами: добавление, просмотр, удаление и базовая организация через локальный JSON-файл. Поддерживает аутентификацию пользователя. Весь интерфейс доступен как через консоль, так и через REST API на Go (Fiber не используется — чистый net/http). Данные сохраняются в slang.json.

🚀 Требования
Go 1.21+
🌐 REST API Endpoints
Все эндпоинты возвращают JSON.
Слово проверяется на уникальность без учёта регистра (strings.EqualFold). Удаление происходит по числовому индексу (начиная с 1).

🔐 Аутентификация
Метод
Путь
Описание
POST
/api/register
Регистрация нового пользователя
POST
/api/login
Вход в систему
GET
/api/user
Получить данные текущего пользователя (без пароля)
🔒 Без регистрации/входа нельзя управлять словарём через CLI, но API пока не проверяет сессию — это важно! (В текущей реализации API доступен без аутентификации.)

🗣️ Словарь сленга
Метод
Путь
Описание
GET
/api/entries
Получить все сленговые слова
POST
/api/entries
Добавить новое слово
DELETE
/api/entries/{index}
Удалить слово по индексу (начиная с 1)
❗ Важно: в API нет эндпоинтов для редактирования, поиска по слову или получения одного элемента — только полный список и удаление по номеру.

📥 Примеры использования (curl)
Регистрация пользователя
bash
123
curl -X POST http://localhost:8080/api/register \
  -H "Content-Type: application/json" \
  -d '{"username": "slang_master", "password": "1234"}'
Вход в систему
bash
123
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username": "slang_master", "password": "1234"}'
Получить данные пользователя
bash
12
curl http://localhost:8080/api/user
# Ответ: {"username": "slang_master"}
Получить все слова
bash
1
curl http://localhost:8080/api/entries
Добавить новое сленговое слово
bash
123456789
curl -X POST http://localhost:8080/api/entries \
  -H "Content-Type: application/json" \
  -d '{
    "word": "краш",
    "meaning": "Человек, в которого влюблён кто-то другой",
    "example": "Он мой краш с первого курса",
    "origin": "от англ. crush",
    "synonyms": ["симпатия", "объект обожания"]
  }'
Удалить слово (например, первое в списке — индекс 1)
bash
1
