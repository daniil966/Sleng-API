# My Slang Dictionary — REST API 📚

**Автор:** Кузьмин Даниил ИС-323

## 🎯 Описание
Проект представляет собой консольное приложение с встроенным REST API на языке Go, предназначенное для сбора и управления современным сленгом. Программа написана с использованием модульной структуры, где каждый функционал вынесен в отдельную функцию или обработчик.

**Цель:** реализовать добавление, просмотр и удаление сленговых выражений через консольный интерфейс и HTTP-запросы, с сохранением данных в локальный файл `slang.json`.

## 🚀 Быстрый старт

### Предварительные требования
- Установленный Go (версия 1.19 или выше)
- Git (для клонирования репозитория)

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/slang-dictionary.git
cd slang-dictionary

# 2. Собрать проект
go build -o slang-app main.go

# 3. Запустить приложение


📖 Команды Git для проекта
Начало работы
./slang-app

# Или запустить напрямую
go run main.go

slang-dictionary/
├── main.go          # Основной файл с логикой консоли, API и работы с данными
├── go.mod           # Модуль Go
├── slang.json       # Файл с данными (создается автоматически)
└── README.md        # Документация

Рабочий процесс
# Создание новой ветки для фичи
git checkout -b feature/add-search-functionality

# Коммит изменений
git add main.go
git commit -m "feat: добавить поиск по словарю"

# Слияние с основной веткой
git checkout main
git merge feature/add-search-functionality
git branch -d feature/add-search-functionality

# Отправка на GitHub
git push origin main
Теги и релизы
# Создание тега версии
git tag -a v1.0.0 -m "Первая стабильная версия"
git push origin v1.0.0

# Создание архива проекта
git archive --format zip --output slang-dictionary-v1.0.0.zip main
# Создание тега версии
git tag -a v1.0.0 -m "Первая стабильная версия"
git push origin v1.0.0

# Создание архива проекта
git archive --format zip --output slang-dictionary-v1.0.0.zip main

🖥️ Консольный интерфейс
При запуске приложения доступны следующие команды:
=== Мой Сленг-Словарь ===
1. Зарегистрироваться
2. Войти
3. Выйти
Выберите действие:

После входа:
=== Словарь сленга ===
1. Показать все записи
2. Добавить новую запись
3. Удалить запись
4. Выйти
Выберите действие:

📊 Структура данных
Формат записи (JSON)
{
  "user": {
    "username": "user123",
    "password": "pass123"
  },
  "version": "1.0",
  "entries": [
    {
      "word": "краш",
      "meaning": "человек, в которого влюблен",
      "example": "Он мой краш уже год",
      "origin": "англ. crush",
      "synonyms": ["влюбленность", "предмет обожания"]
    }
  ]
}
🔧 Технические детали
Зависимости
import (
    "bufio"
    "encoding/json"
    "fmt"
    "net/http"
    "os"
    "strconv"
    "strings"
    "sync"
    "time"
)
Потокобезопасность
Используется sync.RWMutex для безопасного доступа к данным
Чтение: RLock() / RUnlock()
Запись: Lock() / Unlock()

Обработка ошибок

Валидация входных данных
Проверка уникальности слов
Обработка ошибок файловой системы
HTTP статус-коды для API

🧪 Примеры использования
Через API
# Получить все записи
curl http://localhost:8080/api/entries

# Добавить запись
curl -X POST http://localhost:8080/api/entries \
  -H "Content-Type: application/json" \
  -d '{
    "word": "чилить",
    "meaning": "расслабляться, отдыхать",
    "example": "Сегодня буду чилить дома",
    "origin": "англ. chill"
  }'

# Удалить запись #2
curl -X DELETE http://localhost:8080/api/entries/2

Через консоль
Выберите действие: 2
Введите логин: daniel
Введите пароль: *****
Успешный вход!

Выберите действие: 1
=== Все записи ===
1. краш - человек, в которого влюблен
   Пример: Он мой краш уже год
   Происхождение: англ. crush
   Синонимы: [влюбленность предмет обожания]

📝 Особенности реализации
✅ Реализованные функции
Регистрация и аутентификация пользователей
CRUD операции через REST API
Интерактивный консольный интерфейс
Постоянное хранение в JSON файле
Потокобезопасность
Валидация входных данных
Защита от дубликатов
🔒 Безопасность

Важно: В текущей реализации пароли хранятся в открытом виде. Для production использования рекомендуется:
Хеширование паролей (bcrypt, scrypt)
HTTPS для API
JWT токены для аутентификации
🚀 Развертывание

Локально
go run main.go
# Сервер доступен на http://localhost:8080
Docker

FROM golang:1.19-alpine
WORKDIR /app
COPY go.mod ./
RUN go mod download
COPY *.go ./
RUN go build -o slang-app .
EXPOSE 8080
CMD ["./slang-app"]


Сборка и запуск:

docker build -t slang-dictionary .
docker run -p 8080:8080 slang-dictionary
